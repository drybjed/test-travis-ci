---

language: 'python'
python: '2.7'

virtualenv:
  system_site_packages: True

before_install: True
install: True

script:
  - sudo apt-get -yq --purge remove 'postgresql*'
  - sudo apt-get -yq install postgresql-9.1
  - sudo ls -lR /etc/postgresql
  - sudo ls -lR /var/lib/postgresql

